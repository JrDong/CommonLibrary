<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.djr.commonlibrary">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".CLApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".step.StepCountActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".bezier.BezierActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".bezier.SecondActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".bezier.ThirdActivity"
            android:screenOrientation="portrait" />

        <receiver android:name=".step.receiver.SystemReceiver">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.BATTERY_CHANGED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".step.StepService$GrayInnerService"
            android:enabled="true" />
        <service android:name=".step.StepService">
            <intent-filter>
                <action android:name="com.service.platform.StepService" />

                <category android:name="android.intent.category.default" />
            </intent-filter>
        </service>

        <activity android:name=".refresh.PullToRefreshActivity"></activity>
    </application>

</manifest>